trigger:
- main   # or master depending on your branch

pool:
  vmImage: 'windows-latest'

steps:
# (Optional) Install Chrome & ChromeDriver – include later if needed
# - powershell: |
#     choco upgrade googlechrome -y
#     choco upgrade chromedriver -y
#   displayName: "Install Chrome & ChromeDriver"

- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'clean test'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/*.xml'

# 🔥 Add this block directly below Maven task (This is your answer)
- task: PublishTestResults@2
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: '**/test-output/testng-results.xml'
    failTaskOnFailedTests: true
    testRunTitle: 'TestNG Results'
